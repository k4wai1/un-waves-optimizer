{
  "name": "un-waves-optimizer",
  "version": "0.1.0",
  "private": true,
  "description": "UN-WAVES: calculador/optimizador de dano de Wuthering Waves. Motor + datos (JSON5) + web (Vite/React).",
  "scripts": {
    "frontend:dev": "npm --prefix app/ww-frontend run dev",
    "frontend:build": "npm --prefix app/ww-frontend run build",
    "test": "npm --prefix app/ww-frontend run build",
    "tools:weapons": "node tools/weapons/generate_weapons.cjs"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "json5": "^2.2.3",
    "ts-node": "^10.9.1",
    "typescript": "^5.7.3"
  }
}
